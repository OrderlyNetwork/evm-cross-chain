import * as ethers from 'ethers';

const eventSignature = 'MessageSent((uint8,uint8,uint8,address,address,uint256,uint256),bytes)'

function monitor(network: string, address: string) {

}

// function getStateChange(network: string) {
//     const rpcUrl = 
//     const rpc = ethers.JsonRpcApiProvider()
// }